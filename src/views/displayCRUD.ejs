<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
    <title>Document</title>
    <style>
        .list-user .btn {
            display: inline-block;
            padding: 2px 4px;
            background-color: #e5e5e5;
            border-radius: 4px;
            text-decoration: none;
            border: 1px solid #000;
            margin-right: 16px;
        }

        .createUser-btn {
            margin: 50px auto;
            display: flex;
            justify-content: center;
        }

        .createUser-btn a {
            display: inline-block;
            padding: 8px 12px;
            background-color: #333;
            color: #fff;
            border-radius: 4px;
            text-decoration: none;
        }
    </style>
</head>

<body>
    <div class="list-user">
        <div class="w3-container">
            <h2>List User Table</h2>

            <table class="w3-table-all w3-hoverable">
                <thead>
                    <tr class="w3-light-grey">
                        <th>ID</th>
                        <th>Email</th>
                        <th>First Name</th>
                        <th>Last Name</th>
                        <th>Address</th>
                        <th>Actions</th>
                        <!-- <th>Actions</th> -->
                    </tr>
                </thead>
                <tbody>
                    <% for(let i=0; i < dataUser.length; i++) { %>
                        <tr>
                            <td>
                                <%= dataUser[i].id %>
                            </td>
                            <td>
                                <%= dataUser[i].email %>
                            </td>
                            <td>
                                <%= dataUser[i].firstName %>
                            </td>
                            <td>
                                <%= dataUser[i].lastName %>
                            </td>
                            <td>
                                <%= dataUser[i].address %>
                            </td>
                            <td>
                                <a href="/detail-crud?id=<%= dataUser[i].id %>" target="_blank">Detail</a>
                                <a href="/edit-crud?id=<%= dataUser[i].id %>" class="btn btn-edit">Edit</a>
                                <a href="/delete-crud?id=<%= dataUser[i].id %>" class="btn btn-edit">Delete</a>
                            </td>
                        </tr>
                        <% } %>
                </tbody>
            </table>
        </div>

        <div class="createUser-btn">
            <a href="/crud">Create User</a>
        </div>
    </div>
</body>

</html>